interface studentInfo {
  name: string;
  marks: number;
  subject?: string;
}

function userData(input: studentInfo) {
  return userData;
}

console.log(
  userData({
    name: "sachie",
    marks: 100,
    subject: "maths",
  })
);
